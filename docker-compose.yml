services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: folio
      POSTGRES_PASSWORD: folio
      POSTGRES_DB: folio
    ports:
      - "55438:5432"
    volumes:
      - folio_pg:/var/lib/postgresql/data

volumes:
  folio_pg:
